@file:Suppress("unused")

package androidx.compose.foundation.layout

/**
 * Compatibility symbol for an explicit import left by the generated Compose screen.
 * Actual Modifier.weight(...) calls inside Row/Column continue to resolve to the
 * standard RowScope/ColumnScope implementation from Compose Foundation.
 */
const val weight: Int = 0
